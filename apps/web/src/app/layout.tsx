import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from '@/components/providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Health Competition - Compete, Track, Win',
  description: 'Join fitness competitions, track your progress, and win prizes. The ultimate health and fitness competition platform.',
  keywords: ['fitness', 'competition', 'health', 'tracking', 'prizes'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
